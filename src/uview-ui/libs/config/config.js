// 此版本发布于2020-11-19
let version = "1.8.2";

const type = ["primary", "success", "info", "error", "warning"];

const config = {
  v: version,
  type,
  version,
};

module.exports.config = config;
